export const initialState = {
    features: []
}

export const addedReducer = (state=initialState,action) => {
    switch (action.type) {
        case "hi":
            
            return
    
        default:
            return(state)
    }
}